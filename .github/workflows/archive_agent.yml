name: News Agent

on:
  workflow_dispatch: # Оставляет вам ту самую кнопку для ручного запуска, если понадобится
  schedule:
    - cron: '0 7 * * *' # Автозапуск каждый день в 07:00 UTC (10:00 по Москве)

jobs:
  run-news:
    runs-on: ubuntu-latest
    # ... дальше идет ваш остальной код (steps, uses, run и т.д.) без изменений
